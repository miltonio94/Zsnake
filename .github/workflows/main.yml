name: Build and Test Program
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    name: Build 
    steps:
      - uses: actions/checkout@v3
        with:
          version: 0.13.0
      - uses: mlugg/setup-zig@v1
      - run: zig build
